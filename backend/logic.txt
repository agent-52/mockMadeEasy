/* follow ups logic */

followups are for theory only for mvp version
they are only injected if isCorrect is true for the question
they belong to the same interview session

Injection happens:

After /evaluate

Before interview is marked fully finished OR in a new phase

endedAt → base round done
StatsCard → evaluation workspace
/evaluate → unlocks follow-ups
Finalization → future step

/*answer route*/
we are recieving interviewQuestionId and response in the request body

//tts server start 
C:\repo\mockMadeEasy\backend\tts-service>venv\Scripts\activate
then 
uvicorn tts_server:app --port 5001